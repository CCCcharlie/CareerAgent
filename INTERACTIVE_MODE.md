# 交互式搜索使用指南

## 🚀 新功能：交互式多次搜索

### 功能概述

程序现在支持**交互式循环搜索**，运行后浏览器保持开启状态，你可以：
- ✅ 连续进行多次岗位搜索
- ✅ 每次搜索后查看结果
- ✅ 随时选择继续或退出
- ✅ 浏览器不会自动关闭

---

## 📋 使用流程

### 1. 启动程序

```bash
python main.py
```

### 2. 首次搜索

程序会自动：
1. 加载简历文件（跳过 .docx 如果未安装 python-docx）
2. 检查 Ollama 连接
3. 启动浏览器
4. 执行第一次搜索

### 3. 搜索结果展示

```
============================================================
🔍 第 1 次搜索
============================================================

🔍 阶段2：搜索岗位
...

📊 阶段3：爬取并评分
...

📁 结果已保存：output/jobs_20260801_223004.json

============================================================
ℹ️  浏览器保持开启状态
============================================================

请选择下一步操作：
  1. 继续搜索（输入回车或 'y'）
  2. 退出程序（输入 'q' 或 'quit'）
  3. 查看帮助（输入 'h' 或 'help'）

👉 请输入选择: 
```

### 4. 交互选项

| 输入 | 操作 | 说明 |
|------|------|------|
| **回车** 或 `y` | 继续搜索 | 清空当前结果，开始新一轮搜索 |
| `q` / `quit` | 退出程序 | 安全退出，浏览器保持开启 |
| `h` / `help` | 显示帮助 | 查看详细使用说明 |
| `Ctrl+C` | 中断 | 立即中断当前操作 |

---

## 🔧 依赖问题处理

### 问题：python-docx 未安装

**现象**：
```
⚠️ 未安装 python-docx，将跳过 .docx 文件
💡 提示：如需解析 Word 简历，请运行：pip install python-docx
⚠️ 跳过 Charlie_resume.docx（需要 python-docx）
```

**解决方案**：

#### 方案 1：安装 python-docx（推荐）
```bash
pip install python-docx
```

安装后重新运行程序，.docx 文件将被正常加载。

#### 方案 2：转换为其他格式
将 `.docx` 文件转换为：
- `.md`（Markdown）- 推荐
- `.txt`（纯文本）

**转换方法**：
1. 用 Word 打开 .docx 文件
2. 另存为 → 选择 "纯文本 (*.txt)" 或手动复制内容到 .md 文件

#### 方案 3：继续使用（仅使用 .md/.txt 简历）
如果不安装 python-docx，程序会：
- ✅ 自动跳过所有 .docx 文件
- ✅ 正常加载 .md 和 .txt 文件
- ✅ 不影响其他功能

---

## 💡 使用技巧

### 1. 多次搜索不同关键词

**场景**：你想搜索不同方向的岗位

**操作**：
1. 第一次搜索：前端开发岗位
   - 修改 `config.yaml` 中的 keywords
   - 运行程序
   - 搜索完成后按回车继续

2. 第二次搜索：后端开发岗位
   - 修改 `config.yaml` 中的 keywords
   - 在程序中按回车
   - 程序会使用新配置进行搜索

### 2. 对比不同简历的匹配度

**场景**：你有两份简历，想看同一岗位对两份简历的评分

**操作**：
1. 第一次搜索：使用 dev_resume.md
2. 查看结果中的 `selected_resume` 字段
3. 临时重命名简历文件，切换默认简历
4. 第二次搜索：使用 ba_pm_resume.docx
5. 对比两次结果的评分差异

### 3. 批量收集岗位信息

**场景**：你想一次性收集多个方向的岗位

**操作**：
1. 启动程序
2. 完成第一次搜索
3. 按回车继续
4. 修改 config.yaml（可选）
5. 重复步骤 2-4
6. 所有结果保存在不同的 JSON 文件中

---

## 📁 输出文件管理

### 文件命名规则
```
output/jobs_YYYYMMDD_HHMMSS.json
```

**示例**：
- `jobs_20260801_223004.json` - 2026年8月1日 22:30:04
- `jobs_20260801_224512.json` - 2026年8月1日 22:45:12

### 文件内容结构
```json
[
  {
    "title": "Graduate Engineer",
    "company": "Plexus",
    "salary": "$80k - $95k AUD",
    "location": "Melbourne, VIC",
    "tags": ["Full-time"],
    "description": "...",
    "url": "https://www.linkedin.com/jobs/view/4434162885/",
    "selected_resume": "charlie_resume",
    "dimension_scores": {
      "technical_skills": 9.0,
      "experience": 8.5,
      "project_background": 9.5,
      "salary_match": 9.0,
      "growth_potential": 9.0
    },
    "score": 9.2,
    "reason": "技术栈高度吻合...",
    "analysis": "1. 简历选择理由：..."
  }
]
```

### 清理旧文件
```bash
# Windows PowerShell
Get-ChildItem output\*.json | Where-Object { $_.LastWriteTime -lt (Get-Date).AddDays(-7) } | Remove-Item

# Linux/Mac
find output/ -name "*.json" -mtime +7 -delete
```

---

## 🐛 常见问题

### Q1: 为什么浏览器不自动关闭？
**A:** 这是设计特性！浏览器保持开启让你可以：
- 手动查看搜索结果
- 验证 AI 提取的信息
- 进行额外的手动操作
- 避免频繁启动浏览器的开销

### Q2: 如何完全退出程序？
**A:** 
1. 在交互提示中输入 `q` 或 `quit`
2. 或者按 `Ctrl+C` 中断
3. 然后手动关闭浏览器窗口

### Q3: .docx 文件被跳过了怎么办？
**A:** 有三种选择：
1. **安装 python-docx**（推荐）：`pip install python-docx`
2. **转换格式**：将 .docx 转为 .md 或 .txt
3. **忽略**：如果已有其他格式的简历，可以继续使用

### Q4: 每次搜索都会生成新文件吗？
**A:** 是的！每次搜索都会生成带时间戳的新文件，不会覆盖之前的结果。

### Q5: 如何清空之前的搜索结果重新开始？
**A:** 
- 程序会在每次搜索前自动清空 `self.jobs` 列表
- 但输出的 JSON 文件会保留
- 如需删除旧文件，手动清理 `output/` 目录

### Q6: 出现 "I/O operation on closed pipe" 错误怎么办？
**A:** 这是 Windows 上的已知问题，不影响功能：
- 程序仍能正常运行
- 浏览器保持开启
- 可以忽略这些警告
- 如需消除，确保正确关闭 asyncio 事件循环

---

## 🎯 最佳实践

### 1. 准备工作
```bash
# 1. 安装依赖
pip install python-docx

# 2. 准备简历
# 将简历放在 data/ 目录下
# 支持 .md, .txt, .docx 格式

# 3. 配置搜索关键词
# 编辑 config.yaml
```

### 2. 运行流程
```bash
# 1. 启动程序
python main.py

# 2. 等待首次搜索完成

# 3. 查看结果
# 打开 output/jobs_*.json 文件

# 4. 决定是否继续
# 输入回车继续，或 q 退出
```

### 3. 结果分析
- 查看 `selected_resume` 了解 AI 选择了哪份简历
- 查看 `dimension_scores` 了解各维度得分
- 查看 `analysis` 了解详细分析过程
- 根据评分筛选合适的岗位

---

## 📝 更新日志

### v2.2 (2026-08-01)
- ✨ **新增**：交互式多次搜索功能
- ✨ **新增**：浏览器保持开启，不自动关闭
- ✨ **优化**：python-docx 依赖检测与友好提示
- ✨ **新增**：帮助系统（输入 'h' 查看）
- 🐛 **修复**：移除异常时的自动关闭逻辑
- 🐛 **修复**：处理 Windows 管道关闭警告

---

## 🤝 反馈与建议

如有问题或建议，欢迎：
1. 查看 [README.md](README.md) 了解基本用法
2. 查看 [OPTIMIZATION.md](OPTIMIZATION.md) 了解性能优化
3. 提交 Issue 报告问题
